import styled from 'styled-components';

const ClusterListWrapper = styled.div`
    position: relative;
    width: 100%;
    height: 200px;
    overflow-y: scroll;
`;

export default ClusterListWrapper;
